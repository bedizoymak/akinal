# Change

- Removed the visible `STALE` badge from the admin market ticker header UI.
- Kept stale detection, fallback rates, tooltip messaging, API contract, polling, source, colors, and layout unchanged.

# Validation

- Passed: `npm run build`

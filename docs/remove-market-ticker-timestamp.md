# Change

- Removed the visible last-updated timestamp from the admin market ticker.
- Kept `EURO`, `DOLAR`, and `GRAM ALTIN` display unchanged.
- Kept existing live refresh, polling, cache-busting, cache, and fallback behavior unchanged.

# Validation

- `npm run build` passed.
- `git status` checked before commit.

# Change

- Moved each ticker percentage badge below its value.
- Top row now contains label and value.
- Bottom row now contains the `%` change badge.
- Kept existing colors, theme, animations, order, API, polling, cache, and data source unchanged.

# Validation

- Passed: `npm run build`
- Checked: `git status`

# Risk

- Ticker is slightly taller to reduce visual crowding while preserving compact header placement.
